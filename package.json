{
  "name": "cplace-browser-extension",
  "version": "0.2.0",
  "private": true,
  "description": "Chrome/Edge extension that detects cplace pages and exposes optional modules.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "build:icons": "node scripts/build-icons.mjs",
    "package": "npm run build:icons && wxt zip",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@webext-core/fake-browser": "^1.3.0",
    "happy-dom": "^20.9.0",
    "jsdom": "^29.1.1",
    "sharp": "^0.34.5",
    "vitest": "^3.2.0",
    "wxt": "^0.20.0"
  }
}
