{
  "name": "cplace-browser-extension",
  "version": "0.9.1",
  "private": true,
  "description": "Chrome/Edge extension that detects cplace pages and exposes optional modules.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "package": "wxt zip && wxt zip -b firefox && wxt zip -b safari",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@webext-core/fake-browser": "^1.3.0",
    "happy-dom": "^20.9.0",
    "jsdom": "^29.1.1",
"vitest": "^3.2.0",
    "wxt": "^0.20.0"
  }
}
